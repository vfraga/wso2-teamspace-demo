# ─── WSO2 Identity Server ───
IS_BASE_URL=https://localhost:9443
# Tenant handle for WSO2 IS. Set to "teamspace" for tenant mode, leave empty for carbon.super
IS_ORG_HANDLE=teamspace
# Path to a clean/fresh installation of WSO2 IS 7.2.0.24 (required ONLY for running live E2E tests)
WSO2_IS_TEMPLATE_PATH=/path/to/wso2is-7.2.0.24

# ─── TLS Verification ───
# Set to false for local dev with self-signed WSO2 certs
IS_VERIFY_TLS=false

# ─── OAuth2 Application ───
# CLIENT_ID / CLIENT_SECRET / APP_ID are generated by `python setup_is.py`.
# Run that script, then paste the printed values here.
CLIENT_ID=<from setup_is.py output>
CLIENT_SECRET=<from setup_is.py output>
APP_ID=<application ID from setup_is.py output>
APP_NAME=Teamspace

# ─── Flask App ───
FLASK_SECRET_KEY=<random-32-byte-hex>
FLASK_HOST=localhost
FLASK_PORT=5001
# Standard Flask flag. `flask run` (used by start.sh) auto-loads this file and
# honours FLASK_DEBUG, enabling debug mode + the reloader when set to true.
FLASK_DEBUG=true
# Set to "production" to enable secure session cookies and a strict CSP header.
# FLASK_ENV=production

# ─── Business API ───
BUSINESS_API_URL=http://localhost:9091
# SQLAlchemy connection string (defaults to a local SQLite file if unset)
# DATABASE_URL=sqlite:///teamspace.db
# Comma-separated CORS allow-list for the FastAPI services
# ALLOWED_ORIGINS=http://localhost:5001

# ─── AI Agent (Phase 2) ───
AGENT_SERVICE_URL=http://localhost:8000
AGENT_REDIRECT_URI=http://localhost:8000/callback
GEMINI_API_KEY=<from Google AI Studio>
# Set to true to run the agent without calling Gemini (used by the test suite)
# MOCK_LLM=false

# ─── M2M Service-to-Service Secrets (REQUIRED for production) ───
# There are TWO logical machine-to-machine secrets here, spread across three
# variable names. Each service has its own config loader and names the secret
# from its own point of view, so the same value shows up under more than one
# name — they are NOT one global secret.
#
# Secret A — protects the AI Agent service (webapp → agent):
#   AGENT_INTERNAL_SECRET is read by the webapp (webapp/config.py) and the agent
#   (agent/config.py, where it becomes the agent's own INTERNAL_SECRET). The
#   webapp sends it as the `X-Internal-Secret` header when calling the agent's
#   /chat, /chat/stream, /state, /clear and /agent-token endpoints, which are
#   guarded by agent/main.py:require_internal_secret. The agent ALSO uses this
#   value to HMAC-sign the OAuth `state` JWT during the OBO flow, so it must be
#   set and stable — if left empty the agent generates a random secret at
#   startup that the webapp can't know, which breaks every webapp→agent call.
#   => the webapp and the agent must see the SAME AGENT_INTERNAL_SECRET.
#
# Secret B — protects the Business API (webapp → API and agent → API):
#   INTERNAL_SECRET is read by the Business API (api/config.py) and checked by
#   api/auth.py:require_internal_secret. The callers present the same value
#   under the name BUSINESS_API_INTERNAL_SECRET: the webapp (webapp/api_proxy.py)
#   and the agent (agent/agent_config_cache.py) send it as `X-Internal-Secret`.
#   This is the trust gate that lets a service fetch an org's agent config
#   WITHOUT the user's `view_agent_config` scope, so the OBO flow can start for
#   any user (including non-admins); the user JWT is still forwarded for audit.
#   Used by the webapp chat send_message / send_stream routes (webapp→API) and
#   by the agent's lazy TTL config cache (agent→API).
#   => INTERNAL_SECRET must equal BUSINESS_API_INTERNAL_SECRET (both copies).
#
# Secrets A and B are independent and MAY differ. This template sets all three
# variables to one value purely for convenience.
#
# SECURITY NOTE: this static-shared-secret-in-a-header scheme is a demo
# simplification, NOT a production pattern. The secret never expires, isn't
# scoped per call, and grants full trusted-service access to anyone who leaks
# it. In production, replace it with mTLS or short-lived OAuth 2.0
# client-credentials tokens (issued by WSO2 IS) and always run over TLS.
AGENT_INTERNAL_SECRET=<secret A — random 32-byte hex; guards webapp→agent>
INTERNAL_SECRET=<secret B — random 32-byte hex; guards calls to the Business API>
BUSINESS_API_INTERNAL_SECRET=<must match INTERNAL_SECRET above>

# ─── Default Branding (jsDelivr CDN) ───
# Base URL for the project's demo image assets on the CDN. The setup scripts
# derive their logo/favicon URLs from it (Teamspace and the Worklink IdP).
# CDN_IMG_BASE_URL=https://cdn.jsdelivr.net/gh/vfraga/wso2-teamspace-demo@main/webapp/static/img
# Optional — fall back logo/favicon used when an org has no custom branding.
# DEFAULT_LOGO_URL=https://cdn.jsdelivr.net/gh/vfraga/wso2-teamspace-demo@main/webapp/static/img/teamspace-logo.svg
# DEFAULT_FAVICON_URL=https://cdn.jsdelivr.net/gh/vfraga/wso2-teamspace-demo@main/webapp/static/img/teamspace-favicon.svg

# ─── Federated Identity Provider (optional — external-IdP / SSO demo) ───
FEDERATED_IDP_URL=https://localhost:9444/t/worklink.com/scim2/Users
FEDERATED_IDP_ADMIN_USER=teamspaceadmin@worklink.com
FEDERATED_IDP_ADMIN_PASSWORD=
# Branding for the Worklink IdP login page on :9444 (setup_idp_server.py).
# Logo/favicon default to CDN_IMG_BASE_URL + the worklink-idp-* assets.
# WORKLINK_IDP_LOGO_URL=
# WORKLINK_IDP_FAVICON_URL=
# WORKLINK_IDP_PRIMARY_COLOR="#155E75"
# WORKLINK_IDP_SECONDARY_COLOR="#D7EEF5"

# ─── Setup-Script Credentials ───
# Consumed by setup_is.py / setup_idp_server.py / setup_secondary_is.py.
# IS_SUPER_ADMIN_PASSWORD and IS_TENANT_ADMIN_PASSWORD are REQUIRED to bootstrap;
# without them the IS API calls return 401.
IS_SUPER_ADMIN_USERNAME=admin
IS_SUPER_ADMIN_PASSWORD=<WSO2 IS super-admin password>
# Becomes the teamspaceadmin tenant admin password — this is your portal login.
IS_TENANT_ADMIN_PASSWORD=<choose-a-password>
# Password for the federated IdP's seeded test users (setup_idp_server.py).
FEDERATED_USER_PASSWORD=<choose-a-password>
